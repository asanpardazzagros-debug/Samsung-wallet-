<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Samsung Vault - Modern Logo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        
        .logo-container {
            background: white;
            border-radius: 25px;
            padding: 40px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
            text-align: center;
            max-width: 500px;
            width: 100%;
        }
        
        .dynamic-lock {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            position: relative;
            animation: float 3s ease-in-out infinite;
        }
        
        .lock-body {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #1428A0, #00a3ff);
            border-radius: 20px;
            position: absolute;
            top: 20px;
            left: 20px;
            box-shadow: 0 10px 25px rgba(20, 40, 160, 0.3);
        }
        
        .lock-hole {
            width: 25px;
            height: 25px;
            background: white;
            border-radius: 50%;
            position: absolute;
            top: 15px;
            left: 27.5px;
        }
        
        .lock-handle {
            width: 40px;
            height: 20px;
            background: white;
            border-radius: 10px 10px 0 0;
            position: absolute;
            top: 0;
            left: 20px;
        }
        
        .security-rings {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        
        .ring {
            position: absolute;
            border: 2px solid #1428A0;
            border-radius: 50%;
            animation: pulse 2s ease-in-out infinite;
        }
        
        .ring-1 { width: 100px; height: 100px; animation-delay: 0s; }
        .ring-2 { width: 120px; height: 120px; animation-delay: 0.5s; }
        .ring-3 { width: 140px; height: 140px; animation-delay: 1s; }
        
        .logo-text {
            margin-top: 20px;
        }
        
        .samsung {
            font-size: 2.5rem;
            font-weight: 800;
            color: #1428A0;
            letter-spacing: 3px;
            margin-bottom: 5px;
        }
        
        .vault {
            font-size: 3rem;
            font-weight: 300;
            color: #333;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }
        
        .tagline {
            color: #666;
            font-size: 1.1rem;
            letter-spacing: 1px;
        }
        
        .features {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        
        .feature {
            background: #f8f9fa;
            padding: 10px 20px;
            border-radius: 20px;
            font-size: 0.9rem;
            color: #1428A0;
            font-weight: 600;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
            50% { opacity: 0.5; transform: translate(-50%, -50%) scale(1.1); }
        }
    </style>
</head>
<body>
    <div class="logo-container">
        <div class="dynamic-lock">
            <div class="security-rings">
                <div class="ring ring-1"></div>
                <div class="ring ring-2"></div>
                <div class="ring ring-3"></div>
            </div>
            <div class="lock-handle"></div>
            <div class="lock-body">
                <div class="lock-hole"></div>
            </div>
        </div>
        
        <div class="logo-text">
            <div class="samsung">SAMSUNG</div>
            <div class="vault">VAULT</div>
            <div class="tagline">Secure Digital Vault</div>
        </div>
        
        <div class="features">
            <div class="feature">AES-256 Encryption</div>
            <div class="feature">End-to-End Secure</div>
            <div class="feature">Cloud Backup</div>
        </div>
    </div>
</body>
</html>